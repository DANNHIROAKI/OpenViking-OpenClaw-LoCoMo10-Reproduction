# Pinned public evidence excerpt: `examples/openclaw-plugin/README_CN.md` lines 241-274

- Upstream repo: `https://github.com/volcengine/OpenViking`
- Immutable blob URL: `https://github.com/volcengine/OpenViking/blob/2bc65f3/examples/openclaw-plugin/README_CN.md#L241-L274`
- Capture purpose: freeze the current public plugin registration shape.

## Facts recorded from the pinned page

1. The current public plugin id is `openviking`.
2. The plugin is registered on the OpenClaw side as a `context-engine` plugin rather than a `memory` plugin.
3. The public documentation therefore matches the coexistence architecture `memory-core + contextEngine=openviking`, not the historical legacy `memory-openviking` memory-slot route.
