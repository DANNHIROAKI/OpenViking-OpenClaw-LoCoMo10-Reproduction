# Pinned public evidence excerpt: `README_CN.md` lines 660-675

- Upstream repo: `https://github.com/volcengine/OpenViking`
- Immutable blob URL: `https://github.com/volcengine/OpenViking/blob/31577da/README_CN.md#L660-L675`
- Capture purpose: freeze the official OpenClaw plugin result table as public evidence.

## Facts recorded from the pinned page

1. The section title is `OpenClaw 上下文插件详情`.
2. The benchmark is described as LoCoMo10 long-horizon dialogue evaluation with category 5 removed, leaving **1540 cases**.
3. The note states `OpenViking 版本：0.1.18`.
4. The task model is recorded as `seed-2.0-code`.
5. The evaluation script points to `ZaynJarvis/openclaw-eval`.
6. The public table lists four rows:
   - `OpenClaw(memory-core)`
   - `OpenClaw + LanceDB (-memory-core)`
   - `OpenClaw + OpenViking Plugin (-memory-core)`
   - `OpenClaw + OpenViking Plugin (+memory-core)`
7. The table reports completion rate and total input tokens for each row.
